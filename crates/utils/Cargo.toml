[package]
name = "utils"
version = "0.1.0"
edition = "2018"

[dependencies]
log = "0.4.8"
serde = { version = "1.0.104", features = ["derive"] }
typetag = "0.1.4"
bracket-lib = { version = "0.7.0", features = ["specs", "serde"] }
specs = { version = "0.16.1", features = ["specs-derive"] }
